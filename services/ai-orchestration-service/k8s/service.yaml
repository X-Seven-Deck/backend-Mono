apiVersion: v1
kind: Service
metadata:
  name: ai-orchestration-service
  namespace: ${K8S_NAMESPACE}
  labels:
    app: ai-orchestration-service
    tier: backend
spec:
  selector:
    app: ai-orchestration-service
  ports:
  - port: 8030
    targetPort: 8030
    protocol: TCP
    name: http
  type: ClusterIP
